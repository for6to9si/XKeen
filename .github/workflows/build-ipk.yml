name: Build Xkeen IPK packages

on:
  release:
    types: [created] 
  workflow_dispatch:
  repository_dispatch:
  push:
    tags:
      - 'v*'
      
permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-ipk:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        # arch: [mipsel, mips, aarch64]
        arch: [mipsel]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up environment
      run: |
        sudo apt-get update
        sudo apt-get install -y tar gzip jq

    - name: Read VERSION and set as environment variable
      run: |
        VERSION=$(cat VERSION)
        echo "Текущая версия: $VERSION"
        echo "version=$VERSION" >> $GITHUB_ENV
        
    - name: Распаковка xkeen.tar
      run: |
        mkdir -p /opt/sbin
        mkdir -p /opt/etc/init.d
        tar -xvf xkeen.tar -C /opt/sbin
        
    - name: Display artifacts
      run: |
        ls -al ./
        ls -R ./
        ls -al /opt/sbin/

    - name: Install xkeen
      run: |
        export PATH="/opt/sbin/:$PATH"
        cd /opt/sbin/_xkeen
        xkeen -i

